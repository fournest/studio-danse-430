framework:
    secret: '%env(APP_SECRET)%'

    session:
        handler_id: null
        cookie_secure: false  # Forcé à false pour éviter les blocages en HTTP local
        cookie_samesite: lax

when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file